include_rules

CFLAGS += -lsfml-graphics -lsfml-window -lsfml-system 
# CFLAGS += -lBox2D
CFLAGS += -std=c++0x
PROGRAM = fuzzy-path

: foreach *.cpp |> !cxx |> 
: obj\*.o |> g++ %f -o %o $(CFLAGS) |> bin/$(PROGRAM).exe